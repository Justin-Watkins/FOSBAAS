# Install FOSBAAS from a locally built tarball.
# Build it first with:  R CMD build .
install.packages("../FOSBAAS_0.2.0.tar.gz", repos = NULL, type = "source")

# Or install straight from GitHub:
# devtools::install_github("Justin-Watkins/FOSBAAS")
